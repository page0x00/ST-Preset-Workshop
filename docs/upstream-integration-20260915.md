# 作者 2.98.12 整合

二改版本：`2.98.12-page0x00.1`。在本地 `二改版` 分支整合作者更新，未推送远端，也未部署到其他酒馆安装目录。

## 上游来源

已执行 `git fetch origin main`。现有 `origin` 和 `mine` 都指向 page0x00 的分叉，其 `main` 仍为 `a52e18bd6fb1f10e2d700f110c0a8ffeefc1e46e`（2.97.36）。因此继续执行 `git fetch upstream main`，从实际作者仓库取得最新代码，保留原 remote 配置。

- 作者：[yui-ovo](https://github.com/yui-ovo/ST-Preset-Workshop)。
- 最新提交：[bc7e746b2b8c48c123e1035be134a0f744418cf7](https://github.com/yui-ovo/ST-Preset-Workshop/commit/bc7e746b2b8c48c123e1035be134a0f744418cf7)，版本 2.98.12。
- 提交时间：2026-09-15 14:26:09 +08:00。
- 提交说明：`fix: disable unrecorded entries when applying snapshots (2.98.12)`。
- 合并基线：此前已整合的作者 `2669ad8`（2.98.8）；本地起点 `b6403f8`。

## 完整接入的作者更新

| 提交 | 最终行为 |
| --- | --- |
| `ac9b1fa` | 收藏多选拖入文件夹、拖回根目录按批次保存，避免逐条并发保存互相覆盖。 |
| `fc87945` | 批量拖放兼容更新后仍存活的旧收藏 store；旧接口回退采用串行调用。 |
| `b8e58b9` | 支持条目前后、文件夹边缘等插入点；批量移动采用可见顺序，正确更新归属与根目录混合排序。 |
| `bc7e746` | 应用快照关闭未记录条目；ID 已匹配的记录不会再次套用到同名新条目；无条目或分组匹配的失效快照在写入前拒绝。 |

快照取消／编辑还原仍采用局部还原语义，不关闭期间新增的条目；作者新版轻量编辑器、默认快照、角色／聊天绑定、当前预设持久化等功能全部保留。

## 冲突与二改差异

4 个冲突文件均已解决：`dist/index.js`、`dist/workshop-v3.02.js`、`manifest.json`、`package.json`。压缩业务段按保留原始文本的 token 三方合并，并逐段复核。版本统一更新，作者测试命令完整保留，另保留二改专用测试入口。

收藏存储移动函数、收藏 store、条目拖放路由、收藏面板处理函数及快照匹配／应用函数均与作者版本逐字一致。作者本轮更新的 6 个测试文件也逐字保留。

已移除旧的逐条并发收藏拖放调用和旧快照应用分支，使用作者最终实现。本轮涉及的业务区域此前没有额外的同功能二改补丁，因此没有再添加补丁，也没有为了减小行数移除仍有作用的自定义。继续保留：

- 中控、四种美化、条幅宽度与正文可读性。
- 当前酒馆预设的权威选择、草稿恢复、延迟续载。
- 安卓拖动缓存、按帧合并、局部监听、空闲停止扫描和运行时清理。
- 聊天期间避免自动刷新，以及独立尺寸、悬浮吸附、主题过渡性能优化。

`dist/workshop-theme-system.js` 整份文件与整合前一致，Aqua/Violet token、冻结 CSS、获准例外及冻结视觉 fixture 均未修改；没有新增共享 CSS。用户未跟踪文件 `preset-v2.48-p39.json` 未修改、未纳入提交。

## 验证

- `npm run check`：作者 precheck、check、世界书 postcheck 全部通过，共 132 次测试脚本调用。
- `npm run check:ui`：10 项二改回归通过，覆盖冻结视觉、草稿、条幅、中控、聊天保护、延迟渲染、拖动与清理。
- `npm run test:worldbook-unsaved-target`：未保存目标草稿及重复添加、同名与插入顺序通过。
- 作者新增收藏测试覆盖一次保存、文件夹归属、可见顺序、过期选择、自身落点及无变化时不保存。
- 作者新增快照测试覆盖手动／默认／角色／聊天应用、工坊草稿与原生入口、已命名预设及 `in_use` 持久化、同名匹配、失效保护、分组及取消还原。
- `npm run test:banner-browser`：真实 Chromium 5 类视口，宽度保存／重载、裁切、正文可读性及材质保持通过。
- `npm run test:phone-performance-browser`：手机横竖屏和桌面通过；拖动预览无几何／样式读取、无逐帧持久化，保存／取消／重载和静止材质恢复正常。

- `npm run test:floating-dock-browser`：手机、平板横竖屏及桌面共 5 类视口通过，涵盖向内脱离、取消、重新吸附、重载、即时打开、原生边缘形状和手势期间零几何读取。
- `git diff --check`、冲突标记、版本一致性和冻结文件哈希检查通过；结束前再次 fetch，作者最新仍为 `bc7e746`。

### 作者历史测试遗留

额外单独运行作者本轮修改的 `test-test46-switch-snapshots.mjs`、`test-test47-switch-snapshot-entry.mjs`、`test-test55-snapshot-draft-transaction.mjs`，发现它们仍断言旧相机入口或录制模式禁用保存按钮。已在纯作者 `bc7e746` 临时副本复现完全相同的失败；这三个脚本不在作者当前 `npm run check` 流程内。

保留这些作者文件原样并记录情况。新版快照行为由作者当前有效测试和二改轻量编辑器草稿隔离回归验证，不恢复已被作者移除的旧录制实现。

浏览器回归运行发布 CSS 和真实前端模块的测试页面，没有启动完整酒馆聊天后端；安卓实机触感与完整宿主联调仍需实际使用确认。

[本轮验证摘要](verification/upstream-integration-20260915.json)。
